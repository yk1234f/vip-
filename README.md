# vip视频解析线路优选助手

维护者：**yk1234f**。版本：**3.9.8**。

安装文件：`vip视频解析线路优选助手.user.js`。这是采用独立名称和 namespace 的修改版，来源与许可证情况见 `SOURCE_NOTICES.md`，改名不能消除上游授权义务。

## 界面示例

当前界面支持播放状态、分阶段进度、结果筛选、健康详情和密度切换。下图为最新版在本地浏览器中的界面示例，使用演示数据，不代表在线线路实测结果。

![线路优选界面](https://raw.githubusercontent.com/yk1234f/vip-/main/card-design-v3.9.4.png)

当前保留11个入口；已确认重复的入口已移除，但不能保证余下后端全部独立或始终可用。详见 [上游审计](UPSTREAM_AUDIT.md)。

安装后刷新视频页，使新版逻辑生效。播放卡顿的实际站点根因尚未确认；3.9.3提供被动诊断与全新载入，3.9.4修复检测期间滚动回顶。

## 3.9.8 更新

提高正片时长说明文字的亮度，与正文使用一致的浅色文字，保留字号和布局。

## 3.9.7 更新

移除手动载入提示、重新载入和播放诊断工具栏。管理和重新计算健康记录组合为不可拆行按钮组。测试期间尝试一次触发明确标注“点击播放”的可见播放按钮，并保持静音检测；不点击确认链接，也不能替代浏览器要求的真实用户点击。最终播放器仍全新载入。

## 3.9.6 更新

“重新计算健康记录”移到线路列表上方，替换外层“近期健康排序”，管理模式沿用同一个按钮，不再重复。健康排序入口移入管理模式；重新计算仍保留累计次数、最近成功时间及手动顺序，测试期间禁用。

## 3.9.5 更新

初测仅一条通过时跳过复测；两条及以上才复测择优。最终胜者（包括提前选线）全新载入，释放全部测试播放器，不继承测试进度或静音状态；线路或浏览器限制自动播放时仍需点击播放。

## 3.9.4 更新

修复测试刷新重建列表导致滚动回顶：刷新前后保留滚动位置，关闭浏览器自动滚动锚定；检测期间固定本轮卡片顺序，避免健康分更新引起位置跳动。测试结束后恢复正常排序。

## 3.9.3 更新

增加胜出后最长120秒的被动播放诊断（仅保留最近20个样本），记录暂停/等待事件、缓冲、播放时间及错误码，不干预播放。支持全新载入当前线路以退出测试播放器状态（重置进度）。此次为故障定位与恢复功能，尚未确认用户录屏卡顿的根因，不宣称已修复实际站点问题。诊断仅在面板内显示，不自动上传。

## 3.9.2 更新

胜出播放器忽略已结束会话的迟到测试/暂停指令，提交后不接受原播放器停播指令；原媒体拦截限定当前document，避免跨页面媒体误拦截。识别“请求异常/请求过于频繁”为站点限制，立即结束该线路检测并显示报告域名，不计明确失败。两处类似错误页仍需按实际当前页面追踪，不能仅凭截图判断共用上游。

## 3.9.1 更新

根据2026-09-23浏览器跳转链审计，移除8个共用入口：789解析、ckplayer、playm3u8、盘古、8090归入虾米；爱豆、咸鱼TV、咸鱼新归入七七云。保留11个入口不等于11个已验证独立后端；其中部分暂不可用或上游尚不明确，见 UPSTREAM_AUDIT.md。未加入缺乏有效性证据的新线路。原本地历史数据保留，不合并累计成功次数。

## 3.9.0 更新

新增当前播放状态条、初测/复测分阶段进度、全部/通过/无法检测/未通过筛选、网格健康详情和紧凑/舒适密度（本地保存）。筛选仅影响显示，管理时展示全部线路，测试范围不受筛选影响。提前选线或停止会明确标记，未实测的手动播放不显示虚构评分。

## 3.8.3 更新

增加轻量界面特效：面板淡入、主按钮悬停扫光、卡片微抬起、测试状态呼吸光、正在播放图标柔和闪动、详情展开过渡。支持系统减少动态效果设置，触屏不启用悬停位移；采用实例隔离 CSS 动画，无新增计时器或外部依赖。

## 3.8.2 更新

界面精修：统一内联 SVG 图标、弱化普通卡片边框、突出当前播放线路；开关仅滑块着色，检测结果分行展示，收敛评分和辅助文字层级。保留完整详情及原有检测、统计和存储规则，无新增外部依赖。

## 3.8.1 更新

对齐效果图的横排标题、操作图标、状态胶囊和统计条。新增自动识别/手动填写时长切换，选择自动时清除当前视频的手动时长；中英文输入仍支持。

## 3.8.0 更新

按选定效果图重排：620px 面板、分段播放模式、开关按钮、集中设置区和紧凑横向评分卡片。详细检测文字保留，历史记录按需展开；小于380px切换单列。等待时间选项、中英文时长识别、实测和健康排序规则不变。

## 3.7.1 更新

精简元数据头：移除被全站 @match 覆盖的重复 @include，使版本、说明和结束标记位于文件前部，保持原匹配范围。

## 3.7.0 更新

重新设计深色线路卡片：独立状态标签、实测流畅评分、当前内嵌播放高亮、近期成功率。完整检测过程文字保留，历史健康记录和累计成功次数可展开详情查看。管理与排序仍在列表上方；窄屏自适应，详情展开状态在本次面板刷新时保留。检测、健康排序和存储规则保持不变。

## 3.6.0 更新

新增紫色“无法检测”状态，浏览器自动播放限制、检测器未响应、未找到有效视频时长、未能确认实播等与明确“未通过”分开。检测中的 play() 拒绝原因会上报到主面板，静音重试仍保留；不能保证绕过浏览器要求真实点击的限制。

近期成功率改为通过次数÷明确通过或未通过次数；无法检测、超时及历史 unknown/timeout 不计入分母，不直接扣健康分，另行显示无法检测次数。只有这些不确定记录时显示“暂无样本”。连续超时仍保留作诊断信息，不再作为健康排序扣分项。媒体网络、解码、格式错误及明确片名/时长冲突仍视为本次未通过；不表示线路永久失效。最近30天最多20轮窗口、7天权重衰减、手动排序优先保持不变。以下旧版本说明仅记录当时规则，以本节为准。

## 3.5.3 更新

“管理线路／完成管理”“按近期健康排序”和管理模式下的恢复、重置按钮统一移到统计信息下方、线路列表上方。最终播放器仍优先自动解除静音播放；只有浏览器返回 NotAllowedError 时显示“浏览器限制自动有声播放，点击继续”。播放中断 AbortError 自动重试一次，其他错误不再误报为需要点击。通过原播放器控件恢复有声播放后提示自动消失，不干预之后手动静音。浏览器可能要求在跨域播放器中进行一次真实点击，无法保证免点击有声自动播放。

## 3.5.2 更新

在线路列表上方点击“重新计算健康记录”，清空当前所有线路（包括隐藏线路）的近期样本，成功率显示“暂无样本”，连续超时归零。累计成功次数、最近成功时间、隐藏设置及手动排序保留；测试中禁用此操作。点击“恢复上次健康记录”可恢复最近一次重置前的有效样本，并合并重置后的新结果，仍遵守30天/20轮限制；备份保存在本地。

健康分并非直接按卡片百分比排序：每轮权重为 `0.5^(距今天数/7)`；基础分为 `(通过权重之和+1)/(全部权重之和+2)`，再扣除 `min(0.2,连续超时次数×0.04)×最新一轮权重`。无样本时为中性分0.5。同分按最近成功时间，最后按默认线路顺序。手动排序始终优先，点击“按近期健康排序”才清除手动顺序。

## 3.5.1 更新

线路卡片将“累计可播放”改名为“累计成功次数”，单独显示在“最近成功”下方；统计数据和计数规则保持不变。

## 3.5.0 更新

- 每条线路显示最近成功时间、近期成功率和连续超时次数，数据保存在篡改猴本地存储中，刷新后仍保留。
- 近期成功率取最近 30 天内最多 20 轮已结束检测，显示百分比和“通过轮数/检测轮数”；没有记录显示“暂无样本”。每条线路同一轮只记一次，复测结果和迟到时长排除会覆盖本轮记录。
- 超时、内容或时长不匹配、无法确认均计入成功率分母；取消和未开始不新增记录。这是本机检测通过率，不代表线路永久可用或失效。
- 连续超时按上述窗口内末尾连续超时记录计算，成功或其他已结束结果中断连续超时。最近成功时间表示最近一次实际通过播放检测的时间，即使稍后复测不通过也保留这次实播事实。
- 默认自动排序采用近期表现：以 7 天为半衰期降低旧记录权重，连续超时额外降低优先级；暂无样本的线路采用中性优先级。历史累计次数不再参与排序；健康分相同时再参考最近成功时间。
- 手动拖拽顺序优先保留。点击“按近期健康排序”清除手动顺序，恢复自动排序。旧累计次数保留，近期记录从本版开始积累；不把累计次数转换成虚构的近期记录。
- 统计按线路跨视频、跨支持站点共用；网络状态、测试等待时间及站点限制会影响结果。健康只影响列表和测试队列，最终胜者仍依据本轮实播复测评分。

## 3.4.0 更新

- 前三名复测全部失败时，按初筛评分继续尝试后续候选，直到有一条复测通过或候选耗尽。
- 本轮测试期间持续补读结构化正片时长和腾讯页面数据，B站官方接口失败后间隔重试；读到后重新过滤已通过候选，正在测试的线路使用新时长。选线结束后不继续轮询。旧累计可播放次数不撤回，也不事后补算时长匹配次数。
- 测试中点击已通过线路，立即保留其播放器与进度，终止其余测试；停止测试按钮优先播放已通过的最高分线路，没有可用线路才恢复原播放器。提前观看不保证已经完成二次复测。恢复原播放器按钮保持独立。
- 32项主回归、6项报告器测试通过，覆盖候选递补、时长晚到和提前观看。

## 3.3.1 更新

修复最终胜者继承复测静音的问题：提交胜者时解除静音、清除默认静音属性，音量为零时设为50%；浏览器拒绝有声自动播放时显示点击开启声音按钮。其他候选和原网页保持暂停静音。29项主回归、6项报告器测试通过。

## 3.3.0 更新

- 每条线路独立记录待测、检测中、通过、未通过、无法确认、已取消、复测中，不再凭全局状态或文字推断。
- 初筛完成后，前三名候选依次重新计时复测；其余候选暂停，只有复测通过的候选可成为胜者。暂停不保证第三方后台网络请求全部停止。
- 累计可播放和时长匹配分开统计。时长匹配是可播放次数的子集；旧次数保留为可播放历史，旧记录不补算时长匹配。初筛通过计一次，复测不重复计数。
- 使用唯一的 JSON-LD VideoObject/Movie/TVEpisode 元数据比较片名；双方均提供年份、集数时一并核对，冲突排除。缺失或歧义时显示内容未核验，不保证实际视频内容一致。
- GitHub 安装版使用 Greasy Fork 596803 的更新地址；须待 Greasy Fork 同步新版本后，安装端才会收到对应更新。
- 2026-09-22：29项主回归、5项报告器测试通过，真实Edge本地媒体夹具完成未知时长、原视频静音与二次择优验证。新增线路仍未逐条在真实站点验证播放。

## 3.2.1 更新

拆分芒果TV1聚合入口，新增 TXNP、Playr、DMFLV；虾米不重复添加，M1907保留原入口，2s0继续停用。共19条入口；不代表后端完全独立，新入口尚未验证实播。上方截图为3.2.0界面，线路数量以本版说明为准。

## 安装使用

1. 篡改猴中新建脚本，粘贴安装文件的全部代码并保存。
2. 停用旧的“爱观影视vip通行证”等版本，刷新视频页，避免多个脚本同时操作播放器。
3. 点击悬浮按钮，检查“正片时长”。B 站 BV 视频通过官方信息接口读取当前分 P 的时长；其他站点尝试读取结构化时长。
4. 点击“开始择优”。默认同时检测全部未隐藏入口（目前共 **11 条**）。加载等待可选 30、60、90、120 秒，默认 30 秒；读到匹配时长后，再给最多 45 秒实播观察窗口。也可每批检测 1、3、6 条，降低带宽和内存占用。
5. 等待本轮结束后，先将通过的前三名候选依次复测，再从复测通过者中选出评分最高的一条，关闭其余播放器。通过的候选先暂停等待最终比较。
6. 打开“自动播放”和“按时长优选”后，访问同站视频页会自动启动选线。“自动播放”默认关闭，避免安装后立即在所有视频页启动全量检测。

为检测跨域视频，脚本会在列出的解析站点和匹配 iframe 中运行，不再使用 `@noframes`。为覆盖解析服务动态跳转到的未知嵌套域名，3.2 保留 HTTP/HTTPS 子页面匹配权限。无关顶层页面不显示菜单；子帧只接受受支持顶层页面发出的、绑定窗口和随机会话标识的检测命令。额外连接的官方接口仍只有 `api.bilibili.com`，不使用 `@connect *` 或远程 JavaScript。安装或更新时会出现更广的页面访问权限提示。

## 时长及流畅度

- 时长容差为正片的 2%，最少 3 秒、最多 90 秒。示例 `BV133ew6VEAV` 官方接口返回第 1 P 为 **978 秒（16:18）**，容差约 20 秒，五六分钟的线路不符合。
- 不使用原生 video 的试看/广告时长作为正片标准。读不到可信时长时仍照常测试，完整时长标为未验证；也可手动填写；纯数字表示分钟，也可填 `16:18`、`16分18秒`、`16 min 18 sec`、`Duration: 16:18`、`一小时三十分钟`。
- 候选至少观察 8 秒，有至少 4 秒播放推进、有可播放数据、无已报告媒体错误。进度条跳播不算连续播放。
- 评分依据推进比例、卡顿次数、起播等待、缓冲和丢帧比例。通过后先暂停，测完所有候选再选本轮最高分。
- 线路卡片第一段显示本轮状态和详细过程，末尾显示本地累计通过次数；通过、失败、测试中和未测试分别使用绿色、红色、橙黄色和灰色状态块区分。
- 不再在 8 秒时判定短片失败：0:00、临时短片、广告先继续等待。加载窗口到期仍不符才标为未通过。
- 失败、未知、超时不代表永久失效，不会自动删除线路。检测结果显示在列表及线路悬浮提示中。

短时实播和时长匹配不能保证整部视频每一秒都能播放，也不能证明内容一致。评分反映本轮测试；全量并发分摊带宽，可能影响结果。原网页音视频在内嵌期间持续暂停、静音，恢复播放器时解除监听并还原设置。

## 成功次数与排序

- 每条线路每轮通过检测后增加 1 次成功数，不仅最终胜者计数。
- 单纯点击、新标签页打开或 iframe 加载完毕不计成功。
- v2 的打开次数不会冒充成功次数；新计数单独保存。新 namespace 的存储通常也与旧脚本分离。
- 拖动线路左侧 **↕** 调整顺序，刷新后保留；聚焦按钮后按上下方向键也可移动。检测中暂时禁用拖拽，避免列表更新打断操作。
- “按近期健康排序”清除自定义顺序，按近期表现排序；历史累计次数不参与优先级计算。
- 自定义顺序影响列表及分批测试队列，最终选择仍看本轮复测评分。历史胜者缓存不再抢占手动或健康排序的优先级。

## 边界

拒绝 iframe、验证码、无法注入检测脚本的沙箱页面、站点额外限制或自动播放限制可能导致无法检测，显示未知/超时，不编造成功。测试会访问各第三方线路并发送当前视频网址；B 站链接剔除跟踪参数，仅保留 BV 和分 P 信息。

暂停及 preload=none 不保证停止原站所有 fetch/XHR/MSE 下载。原播放器内的旧 iframe 临时卸载，恢复时重新加载，原进度不保证保留。

## 验证

3.5.0：语法检查、34 项主回归、10 组健康记录测试、6 组报告器及纯函数测试通过。覆盖历史高次数线路降序、手动排序优先、跨刷新读取、超时计数、取消不计数、复测覆盖、迟到时长排除、30 天窗口和最近 20 轮限制。此前实际请求 B 站官方接口成功，示例第 1 P 时长为 978 秒。

3.2 另在真实 Edge 的隔离本地夹具中验证了封闭 Shadow DOM 原视频暂停/静音/音量归零、重复 play 拦截、未知跨域嵌套播放器、未知正片时长回退、实播择优及只保留胜者，均通过。夹具使用真实 HTMLMediaElement 播放合成 WAV，不是仅伪造消息。

**未完成真实篡改猴安装和腾讯站上真实 19 路播放验证。** 模拟测试不证明第三方线路实际都能工作。Greasy Fork 规则页多次访问失败，未完成最新站规逐条核验。

## 构建和测试

```powershell
npm install --cache .\.npm-cache --no-audit --no-fund
python .\build.py
Get-Content -LiteralPath '.\vip视频解析线路优选助手.user.js' -Raw | node --check
Get-Content -LiteralPath '.\test.cjs' -Raw | node --preserve-symlinks
Get-Content -LiteralPath '.\reporter-test.cjs' -Raw | node --preserve-symlinks
Get-Content -LiteralPath '.\health-test.cjs' -Raw | node --preserve-symlinks
```

主体、时长工具、子页面报告器及并发选择器分模块维护，构建后只需安装一个 `.user.js`。jsdom 只用于本地测试。

## 腾讯参考页

以用户提供的 `s4102dlk9wg` 页面为参考，实际抓取当前页面和前端公开脚本，确认当前站使用 Vue/Pinia 的 `union.videoInfoMap[vid]` 保存时长，3.2 保留按当前 VID 读取此数据的路径，最多等待20秒。试看片段的原生 video.duration 不作为正片依据。匿名直连站点元数据接口返回业务错误，尚未从真实页面成功核验该集的正片时长；读不到时照常进行实播择优，并保留中英文手动输入。

安装仍然只需要一个 `.user.js`；开发目录和旧源码包不需要导入篡改猴。

